# known-hosts.txt — 已审域名基线(漂移守卫)
#
# 这是 ci/egress/hostscan 从 claude 可执行文件静态提取、命中遥测/控制面关键词、并经人工分类
# 审过的域名清单。egress-audit 的 drift guard 每次对 claude latest 重扫并 diff 本文件:出现这里
# 没有的新候选 → CI 报错,逼人对新域名分类:
#   • CC 自发遥测/会泄漏设备 IP 或带订阅凭据 → 加进 internal/hosts(MITMHosts 或 PassthroughHosts)收口
#   • MCP 端点 / 功能控制面 / staging 死域 → 本就该本地直连,加进本文件对应分组(配一行分类注释)即可
#
# 定位(见 ci/egress/hostscan/main.go 头注、docs/SUBSCRIPTION-FORWARDING.md):这是「升级即逼人复核
# 新候选」的最强收紧,不是穷尽保证——不含已知关键词的全新遥测厂商域名会漏,候选仍需人读上下文裁决。
#
# 基线对应版本:Claude Code 2.1.170。CI 跑 latest,版本前进出现新域名属正常,复核后更新此文件。
# 空行与 # 行被 diff 忽略;域名行不要加行内注释。

# ── 已在 internal/hosts 收口(经 cc-mysub 出口) ────────────────────────────────
# 推理 + OAuth token 使用(MITMHosts,换真 token)
api.anthropic.com
# CC 自发遥测:datadog 日志 intake(PassthroughHosts,盲隧道,DD-API-KEY 非用户 token)
http-intake.logs.us5.datadoghq.com
# 自动更新二进制/插件下载(PassthroughHosts,盲隧道,公开 CDN 无认证)
downloads.claude.ai

# ── 控制面 / 账户 / web(用户登录态,不带 CC 订阅推理 token → 本地直连) ───────────
anthropic.com
www.anthropic.com
claude.ai
claude.com
code.claude.com
platform.claude.com

# ── 文档 / 状态页 / 支持(静态信息,直连) ──────────────────────────────────────
docs.anthropic.com
docs.claude.com
status.anthropic.com
status.claude.com
support.anthropic.com
support.claude.com

# ── MCP 端点(用户显式配置的集成,带用户自己的凭据、不带订阅 token → 直连) ─────────
# api.datadoghq.com = datadog 官方 MCP server,需用户配 DD-API-KEY/APP-KEY 才启用(非 CC 遥测)
api.datadoghq.com
gcal.mcp.claude.com
gmail.mcp.claude.com
slack.mcp.claude.com
microsoft365.mcp.claude.com
mcp.sentry.dev
mcp-proxy.anthropic.com

# ── 内容 bridge / staging / 内部域(生产 CC 不连或用户内容,非订阅流量 → 直连) ──────
bridge.claudeusercontent.com
bridge-staging.claudeusercontent.com
api-staging.anthropic.com
beacon.claude-ai.staging.ant.dev
claude-ai.staging.ant.dev
claude-staging.fedstart.com
claude.fedstart.com
